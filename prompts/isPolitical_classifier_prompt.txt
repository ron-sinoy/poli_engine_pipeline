Classify each gold-level incident using the exact order below.

Rules:
- Return valid JSON only.
- Return only the exact top-level object shape described below.
- Do not add explanations, markdown, or extra keys.
- Use only the incident content and the provided threads.
- Do not inspect thread keys before step 1.

Process each incident in this order:
1. isPolitical - decide whether the given incident is really political. Use only the incident itself. Do not check the threads or their keys for this decision.
2. Compare the political incident with the threads in its `Threads` list and decide whether it continues any of them. Choose at most one. Pick it only if a reader would consider the incident part of that same continuing story - sharing a topic, a politician, or an institution is not enough on its own.
3. Paraphrase the given content.

Non-political handling:
- If the incident is not political, do not perform steps 2 or 3.
- Put only its source_id in the `non-political` list.

Political handling:
- Include `para_content`, `source_id`, and `thread_id`.
- Set `thread_id` to the `thread_id` of the thread it continues, taken exactly from its `Threads` list.
- If no thread in the list matches, set `thread_id` to null. Never invent a thread_id that is not in the list.
- Do not score or rank the threads. Similarity is measured separately and is not yours to estimate.

Return exactly this shape:
{
  "political": [
    {
      "para_content": "string",
      "source_id": "string",
      "thread_id": 0
    }
  ],
  "non-political": ["source_id"]
}

Input:
gold_level_items:
